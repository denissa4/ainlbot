# Python 3.13 (Debian trixie system python). Versions resolved together and
# functionally tested - see the chart/plotly notes below.
#
# kaleido is held at 0.2.1 deliberately: it bundles its own headless chromium,
# whereas kaleido 1.x requires Google Chrome to be installed in the image. Adding
# Chrome would add a large, CVE-heavy surface, and AWS Marketplace rejects images
# with known vulnerabilities. plotly must stay on 6.x for that pairing - plotly 7
# refuses to export images with kaleido < 1.0.
#
# openai is held at 0.28.0 because the code uses the pre-1.0 API
# (openai.ChatCompletion.acreate, api_base/api_type/api_version). It installs and
# imports cleanly on 3.13; moving to openai >= 1 is a separate code change.
matplotlib==3.11.1
plotly==6.9.0
PyJWT==2.13.0
kaleido==0.2.1
pandas==3.0.5
snowflake-connector-python==4.7.3
redshift-connector==2.1.16
aioodbc==0.5.0
aiomysql==0.3.2
Flask-Cors==6.0.5
requests==2.34.2
Flask==3.1.3
gunicorn==26.2.0
botbuilder-schema==4.17.1
aiopg==1.4.0
google-cloud-bigquery==3.45.0
google-auth==2.57.1
azure-identity==1.25.3
asyncpg==0.31.0
openai==0.28.0
markdown==3.10.3
scipy==1.18.1
numpy==2.5.2
